Ext.define('RaterDashboard.store.FBHomePageFeeds', {
  extend: 'Ext.data.Store',
  config: {
    model: 'RaterDashboard.model.FBHomePageFeeds',
    //storeId: 'newsStore',
    //autoLoad: true,
  }
});